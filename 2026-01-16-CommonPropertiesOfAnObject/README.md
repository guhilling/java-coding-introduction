# Plan for Common Object Properties

## Class recap

* Class
* Interface
* Abstract Class

## What can we do with an _Object_?

* Print it (`toString()`).
* Compare it (`equals()`).
  * Why do we need this?
  * Let's take a look at the `Position` class.

## Recap Exceptions

* "Checked" vs. "Unchecked".
* Stacktraces.

## Introduce Collections

* See `ChessGame` as an example.
  * How to track the moves?
  * Use a `List`!